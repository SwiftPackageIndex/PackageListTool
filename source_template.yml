categories:
  - name: Community Showcase
    anchor: community
    description: "Descriptions can include [Markdown links](https://example.com) and **basic formatting**."
    more:
      title: More packages
      url: https://example.com/more/packages
    packages:
      - identifier: owner/repo1
      - identifier: owner/repo2
      - identifier: owner/repo3
        reason: "Reasons are optional and per-package, not per-category."
      - identifier: owner/repo4
        reason: "Reasons can also include [Markdown](https://example.com)."
  - name: Networking
    anchor: networking
    description: "Descriptions are required, but 'more' links are optional."
    packages:
      - identifier: owner/repo1
      - identifier: owner/repo2
      - identifier: owner/repo3
